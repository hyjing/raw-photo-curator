# Roadmap

RAW Photo Curator 的方向是：**用户定义明确规则，程序在本机学习其余偏好；照片、反馈和
个人模型不离开设备。** 项目不训练新的通用视觉大模型，而是复用可替换的预训练组件，
重点解决 RAW 工作流、可控标准、小样本个人排序和可解释决策。

每个阶段必须达到验收标准再进入下一阶段，避免继续堆叠没有验证价值的 UI 或模型功能。

## Phase 0 — 可恢复的本地分析管线

目标：同一张照片只分析一次，任务可以暂停、恢复和增量更新。

- [x] 为照片建立稳定 ID：路径、文件大小、修改时间和内容抽样指纹
- [x] 将缩略图、EXIF、客观特征和分析器版本写入 SQLite
- [x] 未变化照片命中缓存；新增、修改和删除照片进行增量同步
- [x] 后台任务支持进度、取消、失败后缓存续跑和断点恢复
- [x] 分析器不再逐张打印普通状态，仅输出扫描汇总
- [x] 数据库 schema migration 和缓存失效测试

验收标准：第二次打开 355 张照片目录时，95% 以上照片直接命中缓存；修改一张照片只重算
该照片；中断后可以从上次进度继续。

## Phase 1 — Profile、Criterion 与 Plugin 基础

目标：在增加更多模型前，先建立稳定、可扩展的标准系统。

- [x] 实现 Profile：Travel、Portrait、Landscape、Wildlife 和 Custom
- [x] 将标准分为 `hard_rule`、`soft_weight`、`learned_preference`
- [x] 明确优先级：硬规则 > 用户显式权重 > 本地偏好 > 通用先验
- [x] Criterion 统一输出：分数、置信度、证据、版本和计算成本
- [x] Analyzer Plugin Python Protocol、注册表与按需禁用
- [x] Profile、模型选择和 Custom 权重全部本地持久化
- [x] 可选模型显示下载大小、运行成本、安装状态和隐私边界

验收标准：新增一个标准或分析器不需要修改排序核心；切换 Profile 后可以解释哪些规则和
权重发生变化。

## Phase 2 — 相似照片与连拍组

目标：从跨场景绝对打分转向更真实的组内比较。

- [x] 提取拍摄时间、焦距、曝光和相机序列等 EXIF
- [x] 感知哈希用于近似重复检测
- [x] 接入可替换的轻量视觉 embedding，不自行训练基础模型
- [x] 融合时间距离、视觉距离和 EXIF 建立相似组
- [x] 实现误分组拆分、合并和人工纠正
- [x] 提供同步缩放的组内对比视图
- [x] 记录组内 winner、reject 和比较理由

验收标准：在本地人工标注集上报告分组 precision/recall；用户能够纠正分组且纠正结果被
持久化。

## Phase 3 — 可选客观标准包

目标：复用成熟方法提供可解释标准，不制造一个不透明的“万能审美分”。

### 默认轻量标准

- [x] RAW/JPEG 清晰度、曝光、高光、阴影、对比度、噪声和白平衡
- [x] RAW 线性数据的高光余量与暗部恢复空间（高排名候选按需计算）
- [x] 地平线倾斜、边缘截断和重复程度
- [x] 每项输出置信度、警告和相对组内排名

### 按需启用标准

- [x] 主体和显著性（零下载多尺度局部对比插件）
- [ ] 人脸、眼睛、闭眼与表情
- [x] 主体对焦和背景分离代理（低置信度明确标注）
- [x] 景深与动作时机代理（低置信度明确标注）
- [ ] 通用审美 embedding

验收标准：每项标准都可单独关闭、替换和评测；模型缺失或置信度不足时明确降级，而不是
输出虚假的确定结论。

## Phase 4 — 本地个人排序器

目标：只训练小型用户模型，不训练或微调通用视觉基础模型。

- [x] 使用技术特征、冻结 embedding、EXIF 和组内相对特征构建输入
- [x] 建立固定权重和在线逻辑回归基线
- [x] 实现 pairwise/listwise 轻量排序器
- [x] 支持按 Profile 保存独立的本地 preference model
- [x] 少量反馈时使用强正则和通用先验，避免过拟合
- [x] 显示推荐置信度及主要贡献：硬规则、显式权重或学习偏好
- [x] 支持重置、导出、导入和删除个人模型

验收标准：全部训练在 CPU、本地秒级完成；个人模型文件小于 10 MB；程序能明确区分用户
显式规则和模型推断偏好。

## Phase 5 — 主动反馈与本地评测

目标：减少用户操作，并在不上传照片的情况下证明个性化是否有效。

- [x] 根据不确定性和预期信息增益选择下一组比较
- [x] 平衡高推荐候选、未知候选和场景多样性
- [x] 在用户本地将反馈拆成训练集和留出测试集
- [x] 对比通用先验、固定权重和个人排序器
- [x] 报告 pairwise accuracy、Top-K hit rate、NDCG 和置信区间
- [x] 记录 5、10、20、50 次反馈后的学习曲线
- [x] 匿名聚合统计仅作为明确 opt-in，默认完全关闭

验收标准：产品只在本地报告可复现的改进；如果个性化未超过基线，界面必须如实显示。

## Phase 6 — 工作流集成

目标：成为 Lightroom/Capture One 前的选片层，而不是另一个照片管理孤岛。

- [x] 导出 JSON/CSV 选片清单
- [x] 非破坏性的 XMP rating、label 和 pick/reject 输出
- [x] Lightroom 与 Capture One 兼容性测试
- [x] 批量复制、硬链接或符号链接前提供预览
- [x] 所有文件写操作提供审计记录和撤销方案

验收标准：用户无需在下游软件重复选片；原始 RAW 字节始终不被修改。

## Phase 7 — 发布和生态

目标：让非开发者使用，让模型作者只实现插件而不用重建应用。

- [x] macOS 可安装应用和签名发布流程
- [x] 一条命令的开发环境安装
- [x] 稳定的 Plugin SDK、示例分析器和兼容性测试
- [x] CI 覆盖测试、lint、数据库迁移和打包
- [x] 隐私、性能、模型来源和限制文档
- [x] 英文 README、中文说明、演示视频和真实 benchmark

验收标准：普通用户无需理解 Python 或本地服务器；第三方可以按照文档接入一个新的标准
或模型。

## Implementation order

1. SQLite 特征缓存与文件指纹
2. Profile/Criterion/Plugin 数据模型
3. 相似照片与连拍分组
4. 组内对比 UI
5. 默认轻量客观标准
6. 可选模型插件
7. 本地个人排序基线
8. 主动反馈与本地评测
9. XMP 工作流集成
10. 桌面应用封装

下一项计划任务：**公开发布前，用真实人工反馈完成 Phase 2/5 的摄影师验收数据集。**
